import NavBar from "../NavBar"
import TopBar from "../TopBar"

const Closet = () => {
    return (
        <div className="closet">
            <TopBar/>
            <NavBar/>
        </div>
    )
}

export default Closet