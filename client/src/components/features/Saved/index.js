import NavBar from "../NavBar"
import TopBar from "../TopBar"

const Saved = () => {
    return (
        <div className="outfisavedts">
            <TopBar/>
            <NavBar/>
        </div>
    )
}

export default Saved